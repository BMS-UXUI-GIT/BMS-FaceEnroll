# DESIGN.md — ระบบออกแบบของ FaceCheck (dashboard flow)

เอกสารนี้อธิบายภาษาการออกแบบ ระบบสี ระยะ และหลัก UX ของหน้าจอในกลุ่ม **แดชบอร์ด**
(`lib/app/modules/dashboard/`) ซึ่งเป็นส่วนที่ยกเครื่องตาม Figma "FaceEnroll" ทั้งหมดแล้ว
หน้าจอกลุ่มอื่น (สแกนใบหน้า ลงทะเบียน ตั้งค่า) ยังใช้ธีมกลาง `Nexus` ตามเดิม

> ต้นทางของแบบ: Figma **FaceEnroll** (`yUGxbBS7MDVUCoZUkFJCId`) วาดบนเฟรมกว้าง **402** (iPhone 17)
> และ web app [BMS-FaceEnroll](https://github.com/BMS-UXUI-GIT/BMS-FaceEnroll) — โทเคนสี/ป้ายสถานะ
> ลอกมาจาก `theme.css` ของเว็บโดยตรง เพื่อให้คนที่ใช้ทั้งเว็บและแอปเห็นสีเดียวกันหมายถึงเรื่องเดียวกัน

---

## 1. ภาษาการออกแบบ

หน้าจอนี้ทำงานให้พยาบาลที่เพิ่งลงเวร ยืนอ่านบนมือถือ มือเดียว ใต้แสงจ้าหรือในวอร์ดที่หรี่ไฟ
ทุกการตัดสินใจด้านหน้าตาอยู่ใต้ข้อจำกัดนั้น

| หลัก | ความหมายในทางปฏิบัติ |
|---|---|
| **สีคือความหมาย ไม่ใช่การตกแต่ง** | เขียว/ส้ม/ม่วง/แดง ผูกกับสถานะเดียวตลอดทั้งแอป ห้ามหยิบไปใช้เพราะ "สวยดี" |
| **ตัวเลขต้องอ่านได้ก่อนตัวหนังสือ** | จำนวนวัน/รายการใช้ฟอนต์ตัวเลขขนาดใหญ่ คำอธิบายเป็นตัวเล็กต่อท้าย |
| **ชั้นเดียวพอ** | ไม่มี dialog ซ้อน dialog · ตัวเลือกอยู่ในแผ่นล่าง (bottom sheet) หรือแท็บที่เห็นครบในจอเดียว |
| **งานค้างต้องกดต่อได้ทันที** | ทุกที่ที่บอกว่า "มีปัญหา" ต้องแตะแล้วไปยื่นคำขอได้เลย ไม่ใช่แค่แจ้งเฉย ๆ |
| **ไม่โกหกผู้ใช้** | สิ่งที่ยังไม่ต่อระบบจริงเขียนบอกตรง ๆ บนหน้าจอ (ต้นแบบ) · ปุ่มที่กดไม่ได้บอกเหตุผล ไม่ใช่หายไปเงียบ ๆ |
| **แอนิเมชันบอกความสัมพันธ์** | ใช้เพื่อบอกว่า "อันนี้มาจากอันนั้น" หรือ "ค่านี้เพิ่งเปลี่ยน" ไม่ใช่เพื่อความหวือหวา |

---

## 2. ระบบออกแบบ (design system)

### 2.1 แหล่งความจริงเดียว: `Dash`

โทเคนทั้งหมดอยู่ใน [`dash_theme.dart`](lib/app/modules/dashboard/dash_theme.dart) คลาส `Dash`
**ห้ามเขียนค่าสีดิบ (`Color(0xFF…)`) ในไฟล์วิดเจ็ต** ยกเว้นไล่เฉดเฉพาะจุดที่มีคอมเมนต์กำกับว่ามาจาก Figma โหนดไหน

### 2.2 การย่อขยายตามจอ

| ฟังก์ชัน | ใช้กับ | สูตร |
|---|---|---|
| `Dash.sp(v)` | ขนาดภาพ ไอคอน ความสูงกราฟ | `v × scale` |
| `Dash.box(v)` | กล่องความสูงคงที่ที่มีตัวหนังสืออยู่ข้างใน | `v × scale × textScale` |
| ระยะห่าง (padding/gap) | — | **ไม่สเกล** ใช้ค่าคงที่ตามกริด 4pt |

- `scale = ความกว้างจอ / 402` หนีบไว้ `0.82–1.10` (SE ย่อ ~15% · จอใหญ่ขยายได้ ~10%)
- `textScale` = ขนาดฟอนต์ระบบ หนีบ `1.0–1.2` ตรงกับ `MediaQuery.withClampedTextScaling` ที่ `DashPage` ครอบไว้
- ทั้งสองค่าตั้งครั้งเดียวที่ต้นเฟรมใน `DashPage` → ทุก `Dash.*` ในเฟรมนั้นใช้ค่าชุดเดียวกัน

> เหตุที่ระยะห่างไม่สเกล: ถ้าสเกลทั้งหมด จอเล็กจะได้ layout ที่ "ย่อส่วน" เท่ากันหมด
> แต่สิ่งที่ต้องการคือ **เนื้อหาเท่าเดิม ช่องไฟเท่าเดิม ตัวอักษร/ภาพเล็กลง**

### 2.3 ตัวอักษร

ฟอนต์มาจาก `Nexus` (แหล่งเดียวทั้งแอป) `Dash` แค่เปลี่ยนสี default

| helper | ฟอนต์ | ใช้กับ | น้ำหนักปกติ |
|---|---|---|---|
| `Dash.tech()` | Noto Sans Thai | หัวเรื่อง ป้าย ค่าในช่อง ปุ่ม | w600 |
| `Dash.body()` | Noto Sans Thai | คำอธิบาย บรรทัดรอง | w500 |
| `Dash.num()` | Nunito (fallback ไทย) | ตัวเลข เวลา สถิติ | w700 |

**สเกลขนาด** (ค่า `size` ก่อนคูณ scale)

| ขนาด | ใช้ที่ |
|---|---|
| 20 | ชื่อหน้าในหัวแอป · หัวเรื่อง hero |
| 16–17 | หัวการ์ดกราฟ · หัวแผง "ภาพรวม" · แถบชื่อตอนยุบ |
| 15 | วันที่ในรายการ · ค่าในตารางเทียบ · ปุ่มหลัก |
| 13–14 | เนื้อความหลัก · ชิป · ปุ่มรอง |
| 11.5–12.5 | คำอธิบาย บรรทัดรอง legend |
| 10–11 | หัวคอลัมน์ตาราง · ป้ายใต้แท่งกราฟ · ตัวเลขในปฏิทิน |

ตัวเลขที่ใหญ่ที่สุดคือ **26** (จำนวนรายการค้างในแบนเนอร์) — จงใจให้เป็นตัวเลขเดียวที่เด่นที่สุดในหน้า

### 2.4 ทำไมไม่ใช้ `ThemeData` ของ Material

`Dash` เป็น static getter ที่อ่าน `Nexus.isDark` ตรง ๆ ไม่ผ่าน `Theme.of(context)` เพราะ
หน้านี้มีชิ้นส่วนที่อยู่ **บนแผงน้ำเงิน** กับ **บนพื้นการ์ด** ปนกันในต้นไม้เดียวกัน
การสลับ ColorScheme ตามบริบทจะต้องซ้อน `Theme` หลายชั้นและอ่านโค้ดยากกว่าการส่ง `onPanel: true` เข้าไปตรง ๆ

---

## 3. สี

### 3.1 พื้นและตัวอักษร

| โทเคน | สว่าง | มืด | ใช้กับ |
|---|---|---|---|
| `bg` | `#F0F6FD` | `#161A23` | พื้นหน้าจอ |
| `card` | `#FFFFFF` | `#222835` | การ์ด แผ่นล่าง แถบปุ่ม |
| `rowBg` | `#F4F6F9` | `#262B36` | การ์ดย่อย/แถวในการ์ด (ทึบ 100% เสมอ) |
| `wash` | `#EBF1FD` | `#1F2A3D` | พื้นฟ้าจาง — ปุ่มที่สุดทาง แถบไฮไลต์ในล้อเลือกเดือน |
| `heroSky` | `#D7F0FF` | `#16283A` | พื้นหัวเรื่องหน้าย่อย |
| `hairline` | `#0000001A` | `#FFFFFF1A` | เส้นขอบ/เส้นคั่นทั้งหมด |
| `ink` | `#111827` | `#E8EBF0` | ตัวอักษรหลัก |
| `sub` | `#252A39` | `#DDE2EA` | ตัวอักษรรอง |
| `muted` | `#667385` | `#9AA5B8` | คำอธิบาย |
| `faint` | `#C3CAD6` | `#5B6678` | ปิดใช้งาน / ค่าว่าง |

### 3.2 น้ำเงินหลัก

| โทเคน | ค่า | ใช้กับ |
|---|---|---|
| `accent` | `#5682E9` | ripple ไอคอน เส้นวันนี้ |
| `accentActive` | `#3382E7` | ปุ่มหลัก ตัวเลขเน้น ขอบวันที่เลือก |
| `panel` | `#3382E7` / มืด `#1B2A4A` | แผงน้ำเงินครึ่งล่าง + หัวหน้าย่อย |
| `accentGradient` | `#8CBFEE → #5D9BDD → #2E6CB3` | ปุ่มลองใหม่ |
| แบนเนอร์ | `#045FA9 → #288AD1` | แผ่นรองใต้แบนเนอร์แจ้งเตือน |
| `band` | `#000000 → #2F2F2F` | แถบ legend ท้ายการ์ดกราฟ (ชิปเป็นสีโปร่ง ต้องมีพื้นเข้มรอง) |

`onPanel(alpha)` = ขาวโปร่ง สำหรับทุกอย่างที่วางบน `panel`

### 3.3 สีสถานะ (semantic) — สำคัญที่สุด

| สถานะ | โทเคน | สว่าง | มืด |
|---|---|---|---|
| ปกติ / ตรงเวลา | `ok` | `#34C759` | `#3DDC77` |
| มาสาย | `warn` | `#FC9709` | `#FFAA33` |
| ออกก่อนเวลา | `info` | `#8D58D3` | `#A97BE8` |
| ลืมออก / นอกพื้นที่ | `bad` | `#EB5757` | `#FF6B6B` |
| ไม่มีเวร | `faint` | เทาอ่อน | เทาเข้ม |

ชุดนี้ใช้ **ค่าเดียวกันเป๊ะ** ใน 5 ที่: แถบสัดส่วนในการ์ดสรุป · วงกลมรายวัน · ช่องปฏิทิน · แท่งกราฟ · ชิป legend
legend ที่สีไม่ตรงกับสิ่งที่วาด คือ legend ที่ผิด

**โหมดมืดสว่างขึ้นเล็กน้อยทุกตัว** เพราะม่วง/แดงเดิมจมไปกับพื้นเข้ม
และช่องปฏิทินในโหมดมืดจำกัด alpha ไว้ `0.14–0.34` (สว่าง `0.30–1.00`) — สีเต็มความเข้มบนพื้นเข้มแสบตาและกลายเป็นสีเลือดหมู

### 3.4 ป้าย (badge) — 3 สีต่อหนึ่งสถานะ

`Dash.webBadge(kind)` / `Dash.webShift(kind)` คืน `(พื้น, สีวงไอคอน, สีตัวอักษร)`
ไม่ใช่สีเดียวปรับความโปร่ง — ลอกจาก `--badge-*` ของเว็บมาทั้งชุด
ชนิด: `ontime` `late` `early` `leave` `noin` `outarea` · เวร: `morning` `afternoon` `night`

---

## 4. ระยะและรูปทรง

### 4.1 กริด

ฐาน **4pt** · ระยะที่ใช้จริง: `2 · 4 · 6 · 8 · 10 · 12 · 14 · 16 · 18 · 22 · 24`

| ระยะ | ใช้ที่ |
|---|---|
| **16** | ขอบซ้าย-ขวาของทุกหน้า (ค่าเดียว ห้ามเบี่ยง) และช่องไฟระหว่างบล็อกใหญ่ |
| 22 | ระหว่างกลุ่มในฟอร์ม (หัวข้อถัดไป) |
| 14 | ขอบในของการ์ดเล็ก · ช่องไฟรอบปุ่มในแถบล่าง |
| 12 | ขอบในของการ์ดย่อย · ช่องไฟระหว่างองค์ประกอบในกลุ่มเดียวกัน |
| 8 | ช่องไฟระหว่างชิป/ช่องที่อยู่ในแถวเดียวกัน |
| 4–6 | ระหว่างบรรทัดที่เกี่ยวข้องกันโดยตรง (หัวเรื่อง ↔ คำอธิบาย) |

### 4.2 มุมโค้ง

| รัศมี | ใช้กับ |
|---|---|
| 100 | pill: ปุ่ม ชิป แท็บเลือกเดือน แถบสัดส่วน |
| 24 | แผงน้ำเงิน (มุมบน) · แผ่นล่าง · การ์ด "การสแกนวันนี้" |
| 16 | การ์ดทั่วไป แบนเนอร์ ช่องกรอก รูปที่แนบ |
| 12 | การ์ดย่อยในการ์ด · รูปย่อในหน้าตรวจสอบ |
| 8 | ช่องปฏิทินความเข้ม |
| 6 | หัวคอลัมน์ที่กดได้ (ripple) |

### 4.3 พื้นที่กดขั้นต่ำ

เป้าหมาย **≈44** ทุกอันที่แตะได้ ถ้าตัวหนังสือเล็กกว่านั้นให้ **ยืด padding ของ `Tappable` ขึ้นไปข้างบน
แล้วหักช่องไฟด้านบนออกเท่ากัน** ตัวหนังสือจึงไม่ขยับแต่พื้นที่กดโตขึ้น
(ตัวอย่าง: หัวคอลัมน์ "วันที่" ใน `fix_list_header.dart` — `_headHit = 14`)

### 4.4 กายวิภาคของการ์ด

```
การ์ดมาตรฐาน  = color: Dash.card · radius 16 · border: Dash.hairline
การ์ดในการ์ด  = color: Dash.rowBg · radius 12 · ไม่มีขอบ
```
การ์ดที่ `clipBehavior: Clip.antiAlias` **ต้องตีเส้นขอบด้วย `foregroundDecoration`** ไม่ใช่
`decoration.border` — ไม่งั้น clip กินเส้นไปครึ่งหนึ่ง

---

## 5. การเคลื่อนไหว

| ระยะเวลา | เส้นโค้ง | ใช้กับ |
|---|---|---|
| 160 ms | default | สลับสถานะของชิป/ปุ่ม (`AnimatedContainer`) |
| 180 ms | — | ครอสเฟดกล่องรายละเอียดวัน |
| 220 ms | easeOutCubic | ยืด/หดความสูงของกล่องรายละเอียด |
| 260 ms | easeOutCubic | สลับเนื้อกราฟระหว่างแท็บ (fade + เลื่อนขึ้น 6%) |
| 450 ms | easeOutCubic | นับตัวเลขขึ้นจาก 0 |
| 520 ms | linear + stagger | ไล่โผล่ข้อมูลในกราฟ (`ChartAppear`) |
| 550 ms | easeOutCubic | แถบสัดส่วนยืดจากซ้าย |

**กติกา**

1. ทุกอย่างที่เปลี่ยนความสูงต้องอยู่ใน `AnimatedSize` + `AnimatedSwitcher` ที่ `layoutBuilder`
   วางตัวเก่าซ้อนที่เดิม (`Stack(topCenter)`) — ไม่งั้นตัวเก่าดันความสูงระหว่างจาง
2. การไล่โผล่ใช้ **ตัวคุมเดียว** (`ChartAppear`) + `appearAt(t, i, n)` ไม่ใช่ controller ต่อชิ้น
   ปฏิทินเดือนหนึ่งมีได้ถึง 42 ช่อง
3. `Hero(tag: kFixTimeHeroTag)` เชื่อมภาพประกอบจากแบนเนอร์บนแดชบอร์ดไปเป็นภาพหัวของหน้ารายการ
   (เทียบเท่า "Use Hero Animation" ใน FlutterFlow)
4. **จางด้วย alpha ของสี ไม่ใช่ `Opacity`** เมื่อค่าเปลี่ยนทุกเฟรม — `Opacity` สั่ง `saveLayer` ทุกเฟรมที่ค่าอยู่ระหว่าง 0–1

---

## 6. หลัก UX ที่ผูกกับหน้าจอจริง

1. **ลำดับการอ่าน = ลำดับความเร่งด่วน**
   การ์ดวันนี้ → ภาพรวม (สรุป → กราฟ) → **แบนเนอร์งานค้างปิดท้าย**
   แบนเนอร์อยู่ท้ายสุดโดยตั้งใจ: อ่านกราฟจบแล้วค่อยเจอสิ่งที่ต้องลงมือทำ ไม่ใช่ถูกขวางตั้งแต่ยังไม่ได้อ่านอะไรเลย

2. **ไม่มี "สถานะว่าง" ที่ว่างจริง**
   ยังไม่แตะวันไหน → บอกวิธีใช้ ("แตะวันที่มีเวรเพื่อดูรายละเอียด")
   ไม่มีรายการค้าง → ไอคอนเขียว + "ไม่มีรายการค้าง" (`DashEmptyState`)
   โหลดไม่ได้ → บอกสาเหตุ + ปุ่มลองใหม่ (`DashErrorState`) — ว่างไม่ใช่ความผิดพลาด จึงไม่มีปุ่มในกรณีแรก

3. **โครงร่างระหว่างโหลดต้องเป็นโครงเดียวกับของจริง**
   `DashboardSkeleton` ใช้ layout เดียวกับหน้าจริงเป๊ะ ข้อมูลมาถึงแล้วหน้าไม่กระโดด
   หัวเรื่องที่เป็นข้อความคงที่แสดงของจริงไปเลย ไม่ต้องรอ

4. **ตัวกรองเดียวคุมทุกมุมมอง**
   ชิป legend ท้ายการ์ดกราฟคุมทั้งแท่งกราฟ วงกลมรายวัน และปฏิทินความเข้ม
   ปิดสถานะไหน = สถานะนั้นหายจากทุกที่รวมถึงยอดรวมใน tooltip
   สถานะปิดใช้ชิปโปร่ง + จุดกลวง + ขีดฆ่า — ให้รู้ว่ายังมีอยู่ แค่ถูกซ่อน

5. **ปุ่มที่กดไม่ได้ต้องบอกเหตุผล**
   สาเหตุที่ขัดกับข้อมูลจริง (มีเวลาเข้าแล้ว → เลือก "ลืมสแกนเข้า" ไม่ได้) ยังขึ้นในลิสต์
   แต่จางลงและเขียนเหตุผลกำกับ — ตัวเลือกที่หายไปเฉย ๆ ทำให้คนหาไม่เจอแล้วงงกว่า

6. **ยืนยันก่อนส่งด้วยการวางเทียบ ไม่ใช่ถามซ้ำ**
   หน้าตรวจสอบวางค่าเดิมกับค่าที่ขอแก้เคียงกัน ค่าเดิมที่จะถูกแทนถูกขีดฆ่า
   แล้วเล่าเป็นลำดับ (ค่าเดิม → โครงร่าง → ค่าใหม่) ให้เห็นว่าคำขอนี้ไปแตะอะไรบ้าง

7. **หัวที่ตรึงต้องไม่กระโดด**
   ช่องไฟหัวแผงโตจาก 14 เป็นความสูง status bar **ตามระยะที่เลื่อนไปจริง** ไม่ใช่สลับทันทีตอนตรึง
   (เดิมสูงเพิ่มรวดเดียว ~70dp เห็นกระตุกชัด)

8. **ฟอร์มถามเฉพาะที่ขาด**
   ช่องเวลาโผล่เฉพาะอันที่ไม่มีข้อมูล — ช่องอ่านอย่างเดียวที่ซ้ำของเดิมทำให้ดูเหมือนแก้ได้ทั้งที่แตะไม่ได้

9. **โหมดมืดไม่ใช่การกลับสี**
   พื้นเข้มขึ้น แต่สีสถานะ *สว่างขึ้น* · ความเข้มของพื้นถูกจำกัดช่วง · ตัวเลขบนพื้นย้อมใช้สีของสถานะแทนสีขาว
   ภาพประกอบ **ห้ามใส่ฟิลเตอร์เร่งความสว่าง** (เคยทำแล้วสีอิ่มจนฉูดฉาด) — ให้ยกพื้นของกล่องที่รองอยู่ให้สว่างขึ้นแทน

---

## 7. สารบัญคอมโพเนนต์

ทุกชิ้นส่วน UI ในโฟลว์นี้เป็นคลาสวิดเจ็ตที่ตั้งชื่อแล้ว อยู่ใน
[`lib/app/modules/dashboard/widgets/`](lib/app/modules/dashboard/widgets/)
ตัวหน้าจอ (`*_view.dart`) ทำหน้าที่ **ประกอบ + ถือ state เท่านั้น** ไม่มี UI ของตัวเอง

### โครงหน้าและระบบ
| คอมโพเนนต์ | ไฟล์ | หน้าที่ |
|---|---|---|
| `DashPage` | `dash_page.dart` | โครงหน้าทุกหน้า: หนีบ text scale · เรียก `Dash.useScale` · Scaffold |
| `DockSpacer` | `dash_page.dart` | ช่องไฟท้ายหน้าที่มี dock ลอย (Shell ใช้ `extendBody`) |
| `DashPageBar` / `DashBackButton` | `dash_page_bar.dart` | แถบหัวหน้าย่อย: ปุ่มย้อนกลับซ้าย ชื่ออยู่กลางเสมอ |
| `StickyPanelHeader` / `FixedHeightHeader` | `sliver_headers.dart` | delegate ของหัวที่ตรึง |
| `Tappable` | `../../widgets/tappable.dart` | ripple แบบ overlay — ไม่แตะ layout/decoration เดิม |

### แดชบอร์ด
| คอมโพเนนต์ | ไฟล์ | หน้าที่ |
|---|---|---|
| `DashAppBar` | `dash_app_bar.dart` | หัวแอปแบบ floating เปลี่ยนสีเมื่อพื้นหลังกลายเป็นแผงน้ำเงิน |
| `TodayCard` | `today_card.dart` | การ์ด "การสแกนวันนี้" ปัดดูได้ทีละเวร |
| `ScanTiles` | `scan_tiles.dart` | ช่องเวลาเข้า/ออกในการ์ดวันนี้ |
| `ShiftScene` / `ShiftHeroFrame` | `shift_scene.dart`, `shift_hero_frame.dart` | ฉากท้องฟ้าตามเวร |
| `PanelHeaderSliver` | `panel_header.dart` | หัวแผงที่ตรึง (หัวข้อ + ปุ่มเดือน + แท็บ) |
| `PanelTitleRow` · `MonthChip` · `RangeTabs` | `panel_header.dart` | ชิ้นส่วนของหัวแผง |
| `showMonthPickerSheet` | `month_picker_sheet.dart` | ล้อเลือกเดือน/ปี |
| `StatSummaryCard` · `StatLegendItem` | `stat_summary_card.dart` | แถบสัดส่วนวันทำงาน + legend |
| `DashChartCard` | `dash_chart_card.dart` | การ์ดกราฟทั้งใบ |
| `PeriodHeader` · `ChartRangeBody` · `ChartLegendBar` · `LegendChip` | `dash_chart_card.dart` | หัวการ์ด · ตัวสลับมุมมอง · แถบ legend |
| `WeekDayStrip` · `DayDot` · `DayDetailSwitcher` | `week_day_strip.dart` | มุมมองรายสัปดาห์ |
| `MonthHeatmap` · `HeatCell` | `month_heatmap.dart` | ปฏิทินความเข้มรายเดือน |
| `WorkBarChart` | `work_bar_chart.dart` | กราฟแท่งรายปี + tooltip |
| `DayDetailCard` | `day_detail_card.dart` | กล่องรายละเอียดของวันที่แตะ |
| `FixActionBanner` | `fix_action_banner.dart` | แบนเนอร์งานค้างปิดท้ายภาพรวม |
| `DashboardSkeleton` · `TodayCardSkeleton` · `DashboardHeroTitle` | `dashboard_skeleton.dart` | โครงร่างระหว่างโหลด |
| `ChartAppear` · `PopIn` · `appearAt` | `chart_appear.dart` | การไล่โผล่ของข้อมูลในกราฟ |
| `SplitDot` · `SplitBox` | `split_painters.dart` | วง/ช่องผ่าครึ่งทแยงสำหรับวันที่มีสองเวร |

### โฟลว์ขอแก้ไขเวลา
| คอมโพเนนต์ | ไฟล์ | หน้าที่ |
|---|---|---|
| `FixHeroBar` | `fix_hero_bar.dart` | หัวเรื่องยุบได้ + ปลายทางของ Hero |
| `FixListHeaderSliver` | `fix_list_header.dart` | แท็บสถานะ + หัวคอลัมน์ที่กดเรียงได้ |
| `FixRequestRow` · `relativeDayLabel` | `fix_request_row.dart` | หนึ่งรายการในลิสต์ |
| `FixDayCard` | `fix_day_card.dart` | การ์ดวันที่ขอแก้ (ใช้ทั้งฟอร์มและหน้าตรวจสอบ) |
| `PillChoice` | `dash_buttons.dart` | ชิปเลือกเวร |
| `TimeField` · `OutlinedTapField` · `FieldStatusDot` · `FieldPlaceholder` | `time_field.dart`, `outlined_tap_field.dart` | ช่องกรอกทรงเดียวกันทั้งฟอร์ม |
| `ReasonField` · `ReasonAvatarStack` · `showReasonSheet` | `fix_reason_picker.dart` | เลือกสาเหตุได้หลายอัน |
| `PhotoAttachRow` · `PhotoTile` · `AddPhotoTile` · `showPhotoSourceSheet` | `photo_attach_row.dart` | แนบรูปประกอบสูงสุด 3 รูป |
| `ReviewHeroSliver` · `ReviewHeroCard` | `review_hero_sliver.dart` | หัวหน้าตรวจสอบที่เล่าลำดับค่าเดิม→ค่าใหม่ |
| `CompareTable` · `CompareItem` | `compare_table.dart` | ตารางเทียบก่อน/หลังแก้ |
| `ReviewReasonCard` | `review_reason_card.dart` | สรุปสาเหตุ + หมายเหตุ + รูป |
| `FloatingActionBar` · `actionBarHeight` | `floating_action_bar.dart` | แผงปุ่มลอยปิดท้ายหน้า |
| `SuccessSplash` | `success_splash.dart` | หน้าสำเร็จเต็มจอ เล่นจบแล้วปิดตัวเอง |

### ใช้ร่วมทุกที่
| คอมโพเนนต์ | ไฟล์ | หน้าที่ |
|---|---|---|
| `DashPillButton` · `DashCircleButton` · `DashIconButton` · `DashNavButton` · `PanelPillButton` | `dash_buttons.dart` | ปุ่มทุกชนิด |
| `SectionLabel` · `TableHeadCell` · `Hairline` | `dash_text.dart` | หัวข้อย่อย หัวคอลัมน์ เส้นคั่น |
| `WebStatusBadge` · `WebShiftBadge` · `WebBadge` | `web_badges.dart` | ป้ายสถานะ/เวร ชุดเดียวกับเว็บ |
| `UnderlineTab` | `underline_tab.dart` | แท็บขีดใต้ (ใช้ทั้งแดชบอร์ดและหน้ารายการ) |
| `Shimmer` · `Skel` | `skeleton.dart` | โครงร่าง + แสงกวาด (ตัวคุมเดียวทั้งหน้า) |
| `DashEmptyState` · `DashErrorState` · `InfoNote` | `dash_empty_state.dart`, `dash_error_state.dart`, `info_note.dart` | สถานะว่าง/ผิดพลาด/หมายเหตุ |
| `SheetGrabber` | `fix_reason_picker.dart` | ขีดจับหัวแผ่นล่าง |

---

## 8. กติกาเมื่อเพิ่มของใหม่

1. **UI ทุกชิ้นเป็นคลาส ไม่ใช่เมธอด** — `class Foo extends StatelessWidget` ไม่ใช่ `Widget _foo()`
   (เมธอดสร้างซ้ำทั้งกิ่งทุกครั้งที่หน้าแม่ rebuild และไม่มีขอบเขต `const`)
2. **ไฟล์วิดเจ็ตห้าม `import` หน้าจอ** ทิศทางเป็น view → widgets เสมอ
   (ยกเว้น `day_detail_card.dart` ที่อ้าง `FixRequestView.sentDates` — ที่เก็บชั่วคราวของต้นแบบ จะหายไปเมื่อต่อ backend จริง)
3. **คอมโพเนนต์ที่ต้องการค่า reactive ให้รับ `DashboardController` เข้ามาทาง constructor**
   แล้วครอบ `Obx` ไว้ใกล้ค่าที่มันฟังที่สุด ไม่ใช่ครอบทั้งหน้า
4. **ห้ามครอบ `Obx` รอบสิ่งที่ไม่ได้อ่านค่า `.obs`** — GetX จะโยน error ออกมาเป็น `ErrorWidget`
   ซึ่งใน release build คือกล่องเทาที่ยืดเต็มพื้นที่ที่เหลือ
5. **สีใหม่ต้องเข้า `Dash` ก่อน** พร้อมคอมเมนต์ว่ามาจากโหนด Figma ไหนหรือ token อะไรของเว็บ
6. **เขียนคอมเมนต์ว่า "ทำไม" ไม่ใช่ "ทำอะไร"** — ทุกค่าที่ดูพิลึก (เช่น `-Dash.sp(58)`, `alpha 0.14`)
   ต้องมีบรรทัดบอกเหตุผล ไม่งั้นรอบหน้าจะถูกแก้กลับ
